package com.example.studentapp.database.questionsdbimport androidx.room.Daoimport androidx.room.Deleteimport androidx.room.Insertimport androidx.room.Queryimport com.example.studentapp.models.Questions@Daointerface QuestionsDaoQuestionsDao {    @Query("SELECT * FROM questions;")    fun getAll(): List<Questions>    @Query("SELECT NOT EXISTS(SELECT * FROM questions WHERE question = :question )")    fun isNotExists(question:String?):Boolean    @Insert    fun insertData(arts: QuestionsData?)    @Delete    fun delete(art: QuestionsData)}